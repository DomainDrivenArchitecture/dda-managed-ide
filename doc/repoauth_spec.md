### The Schema is:
```clojure
(def RepoAuth
  {:password Secret
   :username Secret
   :repo Str})
```

it's located in:
```
dda.pallet.dda-managed-ide.infra.clojure
```
and it's used in:
```
dda-managed-ide
```
